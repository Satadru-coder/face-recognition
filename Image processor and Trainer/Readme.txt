#preprocessing_and_training
For processing and resizing the images and to extract the region of interest from the Images I used Image function from PIL module of python(for processing) and 
OpenCV(to extract region of Interest).
to train the model I have used LBPHFaceRecognizer from Opencv.
